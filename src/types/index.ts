export interface NavItem {
    title: string;
    href: string;
  }
  
  export interface Project {
    title: string;
    description: string;
    progress: number;
    image: string;
  }
  
  export interface ResearchArea {
    title: string;
    description: string;
    icon: string;
  }

  